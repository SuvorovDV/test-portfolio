---
phase: 04-project-showcase
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/components/WebAppPreview.tsx
  - src/components/WebAppPreview.module.css
  - src/components/ProjectCard.tsx
autonomous: true
requirements:
  - SHOW-03
user_setup:
  - "Положить реальные видео/скриншоты в public/previews/<id>.mp4 (H.264, muted, ≤ 6 MB, ≤ 15s) и <id>.webp (800×500, ≤ 60 KB). Пока используется SVG placeholder."
must_haves:
  truths:
    - "WebAppPreview рендерит <video> если videoSrc задан, иначе только <img> poster"
    - "Video автоматически играет при попадании в viewport (intersection ratio ≥ 0.5)"
    - "Video паузится когда уходит из viewport"
    - "При prefers-reduced-motion: reduce — autoplay выключен, показан poster с индикацией 'click to play'"
    - "Все видео: muted, loop, playsinline, preload='none'"
  artifacts:
    - "src/components/WebAppPreview.tsx"
    - "src/components/WebAppPreview.module.css"
    - "ProjectCard.tsx — интегрирован WebAppPreview для type='web'"
---

<objective>
Собрать превью веб-приложений с умным IntersectionObserver-based autoplay, корректной reduced-motion поддержкой, и безопасным fallback на poster-картинку.

Output: Web-карточки показывают живое короткое видео при прокрутке мимо.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-project-showcase/CONTEXT.md
@src/types/project.ts
@src/hooks/useReducedMotion.ts
@src/components/ProjectCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: WebAppPreview.module.css</name>
  <files>src/components/WebAppPreview.module.css</files>
  <action>
  Создать `src/components/WebAppPreview.module.css`:

  ```css
  .wrapper {
    position: relative;
    width: 100%;
    aspect-ratio: 8 / 5;
    background-color: var(--bg);
    overflow: hidden;
    border: 1px solid var(--phosphor-dim);
  }

  .media {
    width: 100%;
    height: 100%;
    display: block;
    object-fit: cover;
  }

  .overlay {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--phosphor);
    background-color: rgba(0, 0, 0, 0.5);
    opacity: 0;
    transition: opacity 0.2s;
    pointer-events: none;
  }

  .overlay[data-show='true'] {
    opacity: 1;
    pointer-events: auto;
  }

  .playHint {
    color: var(--phosphor);
    font-family: var(--font-mono);
    font-size: 1em;
  }
  ```

  ВАЖНО:
  - `aspect-ratio: 8 / 5` резервирует место → CLS = 0 (важно для PERF-05)
  - `.overlay` — полупрозрачный слой для «click to play» hint при reduced-motion
  </action>
  <verify>CSS модуль валиден</verify>
  <acceptance_criteria>
    - `.wrapper` имеет `aspect-ratio: 8 / 5`
    - `.overlay` переключается через `data-show` attribute
  </acceptance_criteria>
  <done>Стили готовы</done>
</task>

<task type="auto">
  <name>Task 2: WebAppPreview.tsx компонент</name>
  <files>src/components/WebAppPreview.tsx</files>
  <read_first>src/hooks/useReducedMotion.ts; .planning/phases/04-project-showcase/CONTEXT.md §Web preview (D-09..12)</read_first>
  <action>
  Создать `src/components/WebAppPreview.tsx`:

  ```tsx
  import { useEffect, useRef, useState } from 'react';
  import { useReducedMotion } from '../hooks/useReducedMotion';
  import styles from './WebAppPreview.module.css';

  interface WebAppPreviewProps {
    posterSrc: string;
    videoSrc?: string;
    title: string;
  }

  export function WebAppPreview({ posterSrc, videoSrc, title }: WebAppPreviewProps) {
    const reduced = useReducedMotion();
    const videoRef = useRef<HTMLVideoElement | null>(null);
    const wrapperRef = useRef<HTMLDivElement | null>(null);
    const [isPlaying, setIsPlaying] = useState(false);

    useEffect(() => {
      // Если видео нет или reduced-motion — не запускаем IntersectionObserver
      if (!videoSrc || reduced) return;
      const video = videoRef.current;
      const wrapper = wrapperRef.current;
      if (!video || !wrapper) return;

      const observer = new IntersectionObserver(
        (entries) => {
          const entry = entries[0];
          if (!entry) return;
          if (entry.isIntersecting && entry.intersectionRatio >= 0.5) {
            video.play().then(() => setIsPlaying(true)).catch(() => {
              // autoplay может быть заблокирован (редко, при muted+playsinline — нет)
            });
          } else {
            video.pause();
            setIsPlaying(false);
          }
        },
        { threshold: [0, 0.5, 1] },
      );
      observer.observe(wrapper);
      return () => observer.disconnect();
    }, [videoSrc, reduced]);

    const handleManualPlay = () => {
      const video = videoRef.current;
      if (!video) return;
      if (video.paused) {
        video.play().then(() => setIsPlaying(true)).catch(() => {});
      } else {
        video.pause();
        setIsPlaying(false);
      }
    };

    return (
      <div ref={wrapperRef} className={styles.wrapper}>
        {videoSrc ? (
          <>
            <video
              ref={videoRef}
              className={styles.media}
              src={videoSrc}
              poster={posterSrc}
              muted
              loop
              playsInline
              preload="none"
              aria-label={`Preview of ${title}`}
              onClick={reduced ? handleManualPlay : undefined}
              // При reduced-motion добавляем controls чтобы можно было play/pause вручную
              controls={reduced}
            />
            {reduced && !isPlaying && (
              <div className={styles.overlay} data-show="true">
                <span className={styles.playHint}>&gt; click to play</span>
              </div>
            )}
          </>
        ) : (
          <img
            className={styles.media}
            src={posterSrc}
            alt={`Preview of ${title}`}
            loading="lazy"
          />
        )}
      </div>
    );
  }
  ```

  ВАЖНО:
  - Threshold `[0, 0.5, 1]` — наблюдатель перестраивается при достижении этих ratio
  - `video.play()` возвращает Promise — обязательно `.catch(() => {})` иначе AbortError в консоли при быстром pause
  - При `reduced-motion: reduce` — `controls` true + click-to-play оверлей. Autoplay выключен.
  - Fallback — только `<img>` (если videoSrc не задан — а сейчас у обоих web-проектов так и есть)
  - `aria-label` на video — SR знает что это за медиа
  </action>
  <verify>typecheck + lint; в dev (при reduced-motion false) видео начинает играть когда видно</verify>
  <acceptance_criteria>
    - useReducedMotion используется
    - IntersectionObserver настроен с threshold [0, 0.5, 1]
    - cleanup observer.disconnect() в return useEffect
    - video имеет: muted, loop, playsInline, preload="none"
    - При reduced-motion — `controls` активен, autoplay выключен
    - Fallback на `<img>` если videoSrc отсутствует
    - video.play() обёрнут в try/promise-catch
  </acceptance_criteria>
  <done>Компонент готов</done>
</task>

<task type="auto">
  <name>Task 3: Интеграция WebAppPreview в ProjectCard</name>
  <files>src/components/ProjectCard.tsx</files>
  <read_first>src/components/ProjectCard.tsx</read_first>
  <action>
  В `src/components/ProjectCard.tsx`:

  1. Добавить импорт: `import { WebAppPreview } from './WebAppPreview';`
  2. В блоке `<div className={styles.preview}>` заменить placeholder на условный рендер:

  ```tsx
  <div className={styles.preview}>
    {project.type === 'web' && (
      <WebAppPreview
        posterSrc={project.posterSrc}
        videoSrc={project.videoSrc}
        title={project.title}
      />
    )}
    {project.type === 'telegram' && (
      <span className={styles.previewPlaceholder}>[telegram preview — pending]</span>
    )}
  </div>
  ```

  (TelegramChatPreview будет интегрирован в 04-03.)

  Сохранить styles.preview из 04-01 — но можно упростить: min-height и центрирование больше не нужны, т.к. WebAppPreview сам держит aspect-ratio. Впрочем, оставим как есть для placeholder'а telegram.
  </action>
  <verify>`npm run dev` — web-карточки показывают placeholder-SVG (или видео если добавлено), TG-карточки всё ещё placeholder</verify>
  <acceptance_criteria>
    - ProjectCard импортирует WebAppPreview
    - type === 'web' → <WebAppPreview /> рендерится
    - type === 'telegram' → остаётся placeholder текст
    - build/lint/typecheck зелёные
  </acceptance_criteria>
  <done>Web превью интегрированы</done>
</task>

</tasks>

<verification>
- [ ] build/lint/typecheck зелёные
- [ ] Web-карточки показывают placeholder-SVG (т.к. реальных видео пока нет)
- [ ] Если добавить `videoSrc` в projects.ts с реальным mp4 в public/previews/ — IntersectionObserver начинает управлять play/pause
- [ ] Проверить в Network: `preload="none"` → video.src не загружается до попадания в viewport
- [ ] Тест при reduced-motion: autoplay не срабатывает, виден controls и click-to-play overlay
</verification>

<output>
`.planning/phases/04-project-showcase/04-02-SUMMARY.md`
</output>
