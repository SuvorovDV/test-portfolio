---
phase: 04-project-showcase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/project.ts
  - src/data/projects.ts
  - src/components/ProjectCard.tsx
  - src/components/ProjectCard.module.css
  - src/sections/ProjectsSection.tsx
  - src/sections/ProjectsSection.module.css
  - public/previews/.gitkeep
  - public/previews/placeholder.svg
autonomous: true
requirements:
  - CONT-03
  - SHOW-01
  - SHOW-02
  - SHOW-05
user_setup:
  - "Добавить реальные скриншоты/видео в public/previews/ (см. 04-02 и 04-03 для форматов). Пока используются placeholder'ы."
must_haves:
  truths:
    - "src/types/project.ts экспортирует discriminated union Project = WebProject | TelegramProject"
    - "src/data/projects.ts экспортирует PROJECTS с 4 записями (2 web, 2 telegram)"
    - "ProjectsSection рендерит все 4 карточки в CSS grid"
    - "ProjectCard переключает preview-компонент по поле project.type (в 04-02/04-03 будут подключены)"
    - "Каждая карточка показывает title, year, stack chips, description, ссылки"
  artifacts:
    - "src/types/project.ts — типы"
    - "src/data/projects.ts — данные"
    - "src/components/ProjectCard.tsx/.module.css"
    - "src/sections/ProjectsSection.tsx/.module.css"
    - "public/previews/placeholder.svg — текст-плейсхолдер в CRT-стиле"
---

<objective>
Заложить типы + данные + карточку + сетку. В этом плане карточки рендерятся **без живых preview** — на их месте placeholder. Плагины preview (WebAppPreview, TelegramChatPreview) подключаются в 04-02 и 04-03.

Output: Сетка 2×2 карточек с метаданными и placeholder'ами на местах медиа.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-project-showcase/CONTEXT.md
@src/types/sections.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project types</name>
  <files>src/types/project.ts</files>
  <read_first>.planning/phases/04-project-showcase/CONTEXT.md §Project type schema (D-05)</read_first>
  <action>
  Создать `src/types/project.ts`:

  ```ts
  export interface ProjectBase {
    id: string;
    title: string;
    year: number;
    stack: string[];
    description: string;
    repoUrl?: string;
  }

  export interface WebProject extends ProjectBase {
    type: 'web';
    liveUrl: string;
    posterSrc: string;
    videoSrc?: string;
  }

  export type ChatMessage =
    | { from: 'bot'; text: string; timestamp?: string; buttons?: string[][] }
    | { from: 'user'; text: string; timestamp?: string };

  export interface TelegramProject extends ProjectBase {
    type: 'telegram';
    botUsername: string;
    botUrl: string;
    chatScript: ChatMessage[];
  }

  export type Project = WebProject | TelegramProject;
  ```

  ВАЖНО: `repoUrl` optional — проекты могут быть приватными.
  </action>
  <verify>typecheck</verify>
  <acceptance_criteria>
    - Discriminated union на `type: 'web' | 'telegram'`
    - ChatMessage — union по `from`
    - Все экспорты named
  </acceptance_criteria>
  <done>Типы готовы</done>
</task>

<task type="auto">
  <name>Task 2: Projects data — 4 записи</name>
  <files>src/data/projects.ts</files>
  <read_first>.planning/phases/04-project-showcase/CONTEXT.md §Owner-provided projects (D-01..04), §Data location (D-17, D-18)</read_first>
  <action>
  Создать `src/data/projects.ts`:

  ```ts
  import type { Project } from '../types/project';

  export const PROJECTS: Project[] = [
    {
      type: 'web',
      id: 'tezis',
      title: 'tezis',
      year: 2025,
      stack: ['React', 'Vite', 'TypeScript'],
      description:
        'Web application — демо-площадка для работы с тезисами. TODO: уточнить описание.',
      liveUrl: 'https://tezis.111.88.153.18.nip.io',
      posterSrc: '/previews/placeholder.svg',
      // videoSrc: '/previews/tezis.mp4', — добавить когда будет запись
    },
    {
      type: 'web',
      id: 'frontend-seven',
      title: 'frontend-seven',
      year: 2025,
      stack: ['React', 'Vercel'],
      description:
        'Web application — фронтенд-эксперимент на Vercel. TODO: уточнить описание.',
      liveUrl: 'https://frontend-seven-omega-17.vercel.app/',
      posterSrc: '/previews/placeholder.svg',
    },
    {
      type: 'telegram',
      id: 'marketplace-kwork',
      title: 'marketplace-kwork',
      year: 2025,
      stack: ['Python', 'aiogram', 'SQLite'],
      description:
        'Demo-маркетплейс фриланс-услуг в Telegram — каталог категорий, профили исполнителей, отклики.',
      botUsername: '@test_marketplace_kwork_bot',
      botUrl: 'https://t.me/test_marketplace_kwork_bot',
      chatScript: [
        { from: 'user', text: '/start', timestamp: '12:30' },
        {
          from: 'bot',
          text: 'Добро пожаловать в marketplace. Выберите категорию услуг:',
          timestamp: '12:30',
          buttons: [
            ['Разработка', 'Дизайн'],
            ['Тексты', 'Переводы'],
            ['Все категории'],
          ],
        },
        { from: 'user', text: 'Разработка', timestamp: '12:31' },
        {
          from: 'bot',
          text: 'Разработка · 42 исполнителя. Отсортировать по:',
          timestamp: '12:31',
          buttons: [['Рейтингу', 'Цене', 'Новизне']],
        },
      ],
    },
    {
      type: 'telegram',
      id: 'ticket-seller',
      title: 'ticket-seller',
      year: 2025,
      stack: ['Python', 'aiogram', 'PostgreSQL'],
      description:
        'Продажа билетов на мероприятия через Telegram — афиша, выбор места, оплата (stub).',
      botUsername: '@demo_ticket_seller_bot',
      botUrl: 'https://t.me/demo_ticket_seller_bot',
      chatScript: [
        { from: 'user', text: '/start', timestamp: '18:02' },
        {
          from: 'bot',
          text: 'Афиша на эту неделю:',
          timestamp: '18:02',
          buttons: [
            ['Концерт · Пт 20:00'],
            ['Спектакль · Сб 19:00'],
            ['Стендап · Вс 20:00'],
          ],
        },
        { from: 'user', text: 'Концерт · Пт 20:00', timestamp: '18:03' },
        {
          from: 'bot',
          text: 'Партер от 2000 ₽ · Амфитеатр от 1200 ₽. Выберите:',
          timestamp: '18:03',
          buttons: [['Партер', 'Амфитеатр']],
        },
      ],
    },
  ];
  ```

  ВАЖНО:
  - Все 4 записи типизированы через discriminated union (TS проверит что поля корректны)
  - Description содержит **TODO** маркер для web-проектов — владельцу нужно уточнить
  - Stack — массив коротких строк, без версий
  - posterSrc указывает на `/previews/placeholder.svg` — будет создан в task 4
  </action>
  <verify>typecheck</verify>
  <acceptance_criteria>
    - PROJECTS содержит ровно 4 записи
    - 2 записи с type: 'web', 2 с type: 'telegram'
    - У всех web-записей есть liveUrl, posterSrc
    - У всех telegram-записей есть botUsername, botUrl, chatScript (минимум 4 сообщения)
    - Все id уникальны и kebab-case
  </acceptance_criteria>
  <done>Данные готовы</done>
</task>

<task type="auto">
  <name>Task 3: ProjectCard компонент + стили</name>
  <files>src/components/ProjectCard.tsx, src/components/ProjectCard.module.css</files>
  <read_first>.planning/phases/04-project-showcase/CONTEXT.md §Project card layout (D-06..08)</read_first>
  <action>
  **ProjectCard.module.css:**
  ```css
  .card {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    border: 1px solid var(--phosphor-dim);
    padding: var(--space-3);
    background-color: transparent;
  }

  .header {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    gap: var(--space-2);
  }

  .title {
    color: var(--phosphor);
    font-size: 1.25em;
    margin: 0;
    text-shadow: 0 0 var(--glow-blur-heading) var(--phosphor);
  }

  .title::before {
    content: '> ';
    color: var(--phosphor-dim);
  }

  .year {
    color: var(--phosphor-dim);
    font-size: 0.9em;
  }

  .preview {
    /* Контейнер-слот для WebAppPreview / TelegramChatPreview */
    min-height: 200px;
    background-color: rgba(51, 255, 51, 0.04);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .previewPlaceholder {
    color: var(--phosphor-dim);
    padding: var(--space-3);
  }

  .description {
    color: var(--phosphor-dim);
    margin: 0;
  }

  .stack {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .stackItem {
    color: var(--phosphor);
  }

  .stackItem::before {
    content: '·';
    margin-right: var(--space-1);
    color: var(--phosphor-dim);
  }

  .stackItem:first-child::before {
    content: '>';
  }

  .actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
  }

  .actionLink {
    color: var(--phosphor);
    text-decoration: underline;
  }
  ```

  **ProjectCard.tsx:**
  ```tsx
  import type { Project } from '../types/project';
  import styles from './ProjectCard.module.css';

  interface ProjectCardProps {
    project: Project;
  }

  export function ProjectCard({ project }: ProjectCardProps) {
    return (
      <article
        className={styles.card}
        aria-labelledby={`project-${project.id}-title`}
      >
        <header className={styles.header}>
          <h3 id={`project-${project.id}-title`} className={styles.title}>
            {project.title}
          </h3>
          <span className={styles.year}>{project.year}</span>
        </header>

        <div className={styles.preview}>
          {/* Preview компоненты будут подключены в 04-02 / 04-03 */}
          <span className={styles.previewPlaceholder}>
            [{project.type} preview — pending]
          </span>
        </div>

        <p className={styles.description}>{project.description}</p>

        <ul className={styles.stack} aria-label="Stack">
          {project.stack.map((tech) => (
            <li key={tech} className={styles.stackItem}>
              {tech}
            </li>
          ))}
        </ul>

        <div className={styles.actions}>
          {project.type === 'web' && (
            <a
              className={styles.actionLink}
              href={project.liveUrl}
              target="_blank"
              rel="noreferrer noopener"
            >
              [live]
            </a>
          )}
          {project.type === 'telegram' && (
            <a
              className={styles.actionLink}
              href={project.botUrl}
              target="_blank"
              rel="noreferrer noopener"
            >
              [open in telegram]
            </a>
          )}
          {project.repoUrl && (
            <a
              className={styles.actionLink}
              href={project.repoUrl}
              target="_blank"
              rel="noreferrer noopener"
            >
              [repo]
            </a>
          )}
        </div>
      </article>
    );
  }
  ```

  ВАЖНО:
  - `<article aria-labelledby>` — семантика для SR
  - `h3` (а не `h2`) — иерархия: `<h1>` глобальный, `<h2>` секции, `<h3>` проекты
  - Placeholder-слот preview — содержит `project.type` (поможет отладить дальше)
  - Все внешние ссылки с `rel="noreferrer noopener"`
  </action>
  <verify>typecheck + lint; ProjectCard отрендерился в DevTools</verify>
  <acceptance_criteria>
    - ProjectCard принимает `{ project: Project }`
    - Использует discriminated union: `project.type === 'web'` показывает [live]
    - `project.type === 'telegram'` показывает [open in telegram]
    - Условный рендер [repo] только если `repoUrl` есть
    - `<article aria-labelledby="project-<id>-title">`
  </acceptance_criteria>
  <done>Карточка готова</done>
</task>

<task type="auto">
  <name>Task 4: ProjectsSection grid + placeholder SVG</name>
  <files>src/sections/ProjectsSection.tsx, src/sections/ProjectsSection.module.css, public/previews/placeholder.svg, public/previews/.gitkeep</files>
  <action>
  **ProjectsSection.module.css:**
  ```css
  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(28ch, 1fr));
    gap: var(--space-4);
    margin-top: var(--space-3);
  }
  ```

  **ProjectsSection.tsx:**
  ```tsx
  import { ProjectCard } from '../components/ProjectCard';
  import { PROJECTS } from '../data/projects';
  import styles from './ProjectsSection.module.css';

  export function ProjectsSection() {
    return (
      <section id="projects" aria-labelledby="projects-heading">
        <h2 id="projects-heading">projects</h2>
        <div className={styles.grid}>
          {PROJECTS.map((project) => (
            <ProjectCard key={project.id} project={project} />
          ))}
        </div>
      </section>
    );
  }
  ```

  **public/previews/placeholder.svg** — очень простой CRT-placeholder (не PNG, SVG легче):
  ```xml
  <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 500" width="800" height="500" aria-hidden="true">
    <rect width="800" height="500" fill="#0a0f0a"/>
    <text x="400" y="260" text-anchor="middle" fill="#33ff33" font-family="monospace" font-size="28" style="text-shadow:0 0 4px #33ff33">&gt; recording pending</text>
    <text x="400" y="300" text-anchor="middle" fill="#1fa81f" font-family="monospace" font-size="16">run screencast and drop at public/previews/</text>
  </svg>
  ```

  **public/previews/.gitkeep** — пустой файл, чтобы папка была в git (хотя уже есть placeholder.svg — .gitkeep можно не создавать отдельно, но для консистентности).

  После этого на странице Projects должны появиться 4 карточки в сетке (пока с placeholder preview текстом).
  </action>
  <verify>`npm run dev` — Projects секция показывает 4 карточки: tezis, frontend-seven, marketplace-kwork, ticket-seller; клик по [live] открывает URL в новой вкладке</verify>
  <acceptance_criteria>
    - ProjectsSection импортирует PROJECTS и рендерит ProjectCard для каждого
    - CSS grid `repeat(auto-fit, minmax(28ch, 1fr))` → на mobile 1 колонка, на desktop 2+
    - public/previews/placeholder.svg существует
    - 4 карточки видны в dev
    - build/lint/typecheck зелёные
  </acceptance_criteria>
  <done>Каркас Phase 4 готов</done>
</task>

</tasks>

<verification>
- [ ] build/lint/typecheck зелёные
- [ ] `npm run dev` → секция Projects показывает 4 карточки
- [ ] На desktop (>= 768px) — минимум 2 колонки
- [ ] Клик [live] на tezis — открывает https://tezis.111.88.153.18.nip.io
- [ ] Клик [open in telegram] на marketplace-kwork — открывает https://t.me/test_marketplace_kwork_bot
- [ ] Каждая карточка показывает title/year/stack/description
- [ ] SR читает карточку как article с accessible name = title
</verification>

<success_criteria>

- Sсетка карточек работает
- Данные изолированы в src/data/projects.ts — легко править
- Preview-слот готов к подключению WebAppPreview (04-02) и TelegramChatPreview (04-03)

</success_criteria>

<output>
`.planning/phases/04-project-showcase/04-01-SUMMARY.md`
</output>
