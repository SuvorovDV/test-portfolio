---
phase: 04-project-showcase
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - src/components/TelegramChatPreview.tsx
  - src/components/TelegramChatPreview.module.css
  - src/components/ProjectCard.tsx
autonomous: true
requirements:
  - SHOW-04
user_setup: []
must_haves:
  truths:
    - "TelegramChatPreview рендерит диалог из chatScript без live bot"
    - "Каждое сообщение имеет timestamp"
    - "Bot сообщения слева (dim border), user справа (phosphor bg)"
    - "Inline keyboard отрисован под сообщением с кнопками если buttons задан"
    - "Есть явный affordance 'preview — not a live bot'"
    - "НЕ используется логотип Telegram, только слово 'bot' и username"
  artifacts:
    - "src/components/TelegramChatPreview.tsx"
    - "src/components/TelegramChatPreview.module.css"
    - "ProjectCard.tsx — интегрирован TelegramChatPreview"
---

<objective>
Отрисовать статичный React-мокап Telegram-диалога для двух TG-ботов. Без iframe, без логотипов — чистый HTML+CSS в phosphor-палитре.

Output: TG-карточки показывают мини-диалог который выглядит похоже на Telegram, но в CRT-стиле.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-project-showcase/CONTEXT.md
@src/types/project.ts
@src/data/projects.ts
@src/components/ProjectCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: TelegramChatPreview.module.css</name>
  <files>src/components/TelegramChatPreview.module.css</files>
  <read_first>.planning/phases/04-project-showcase/CONTEXT.md §Telegram preview (D-13..16)</read_first>
  <action>
  Создать `src/components/TelegramChatPreview.module.css`:

  ```css
  .wrapper {
    display: flex;
    flex-direction: column;
    border: 1px solid var(--phosphor-dim);
    background-color: var(--bg);
    font-family: var(--font-mono);
    font-size: 0.95em;
    min-height: 260px;
  }

  .header {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-2) var(--space-3);
    border-bottom: 1px solid var(--phosphor-dim);
    color: var(--phosphor);
  }

  .avatar {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2em;
    height: 2em;
    border: 1px solid var(--phosphor);
    color: var(--phosphor);
    text-shadow: 0 0 var(--glow-blur-heading) var(--phosphor);
  }

  .botName {
    display: flex;
    flex-direction: column;
    line-height: 1.1;
  }

  .username {
    color: var(--phosphor-dim);
    font-size: 0.85em;
  }

  .messages {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    padding: var(--space-3);
    flex-grow: 1;
  }

  .messageRow {
    display: flex;
    flex-direction: column;
    gap: 2px;
    max-width: 80%;
  }

  .messageRowBot {
    align-self: flex-start;
  }

  .messageRowUser {
    align-self: flex-end;
    align-items: flex-end;
  }

  .bubble {
    padding: var(--space-1) var(--space-2);
    border-radius: 2px;
    line-height: 1.3;
  }

  .bubbleBot {
    background-color: transparent;
    border: 1px solid var(--phosphor-dim);
    color: var(--phosphor-dim);
  }

  .bubbleUser {
    background-color: var(--phosphor);
    color: var(--bg);
  }

  .timestamp {
    color: var(--phosphor-dim);
    font-size: 0.75em;
    opacity: 0.7;
  }

  .keyboard {
    display: flex;
    flex-direction: column;
    gap: 2px;
    margin-top: var(--space-1);
  }

  .keyboardRow {
    display: flex;
    gap: 2px;
  }

  .keyboardButton {
    flex: 1;
    padding: var(--space-1) var(--space-2);
    border: 1px solid var(--phosphor-dim);
    text-align: center;
    color: var(--phosphor);
    background-color: transparent;
  }

  .footer {
    padding: var(--space-2) var(--space-3);
    border-top: 1px solid var(--phosphor-dim);
    color: var(--phosphor-dim);
    font-size: 0.8em;
    text-align: center;
  }
  ```
  </action>
  <verify>CSS модуль валиден</verify>
  <acceptance_criteria>
    - .bubbleBot и .bubbleUser имеют разные визуальные стили
    - .messageRowUser выровнен right (align-self: flex-end)
    - .keyboardButton — рамка + текст, нет background-image
    - .footer имеет border-top (визуальное отделение affordance)
  </acceptance_criteria>
  <done>Стили готовы</done>
</task>

<task type="auto">
  <name>Task 2: TelegramChatPreview.tsx компонент</name>
  <files>src/components/TelegramChatPreview.tsx</files>
  <read_first>src/types/project.ts — ChatMessage type</read_first>
  <action>
  Создать `src/components/TelegramChatPreview.tsx`:

  ```tsx
  import type { ChatMessage } from '../types/project';
  import styles from './TelegramChatPreview.module.css';

  interface TelegramChatPreviewProps {
    title: string;
    botUsername: string;
    chatScript: ChatMessage[];
  }

  export function TelegramChatPreview({
    title,
    botUsername,
    chatScript,
  }: TelegramChatPreviewProps) {
    // Первая буква title для avatar
    const initial = title.charAt(0).toUpperCase();

    return (
      <div className={styles.wrapper} role="img" aria-label={`Dialog preview for ${title} bot`}>
        <header className={styles.header}>
          <span className={styles.avatar} aria-hidden="true">
            {initial}
          </span>
          <span className={styles.botName}>
            <span>{title}</span>
            <span className={styles.username}>{botUsername}</span>
          </span>
        </header>

        <div className={styles.messages}>
          {chatScript.map((msg, idx) => (
            <div
              key={idx}
              className={`${styles.messageRow} ${
                msg.from === 'bot' ? styles.messageRowBot : styles.messageRowUser
              }`}
            >
              <div
                className={`${styles.bubble} ${
                  msg.from === 'bot' ? styles.bubbleBot : styles.bubbleUser
                }`}
              >
                {msg.text}
              </div>
              {msg.timestamp && <span className={styles.timestamp}>{msg.timestamp}</span>}

              {msg.from === 'bot' && msg.buttons && msg.buttons.length > 0 && (
                <div className={styles.keyboard} aria-hidden="true">
                  {msg.buttons.map((row, rowIdx) => (
                    <div key={rowIdx} className={styles.keyboardRow}>
                      {row.map((btn, btnIdx) => (
                        <span key={btnIdx} className={styles.keyboardButton}>
                          {btn}
                        </span>
                      ))}
                    </div>
                  ))}
                </div>
              )}
            </div>
          ))}
        </div>

        <footer className={styles.footer}>preview — not a live bot</footer>
      </div>
    );
  }
  ```

  ВАЖНО:
  - `role="img"` + `aria-label` — целый диалог помечен как изображение для SR (не по слову читает). Альтернатива: полностью DOM и SR читает — но тогда нужно структурировать как live region что плохо. Пометка img — компромисс.
  - `aria-hidden="true"` на .keyboard — кнопки декоративные, SR их пропускает
  - Keyboard кнопки — `<span>`, не `<button>` (они не кликабельные)
  - Первая буква title как fake-avatar (`M` для marketplace, `T` для ticket-seller)
  </action>
  <verify>typecheck + lint</verify>
  <acceptance_criteria>
    - Принимает title, botUsername, chatScript
    - Рендерит header с avatar + title + username
    - Bubbles: bot слева, user справа
    - Inline keyboard только для bot сообщений с buttons
    - Footer: "preview — not a live bot"
    - role="img" на wrapper
    - Никаких ссылок на telegram.org, никаких логотипов
  </acceptance_criteria>
  <done>Компонент готов</done>
</task>

<task type="auto">
  <name>Task 3: Интеграция TelegramChatPreview в ProjectCard</name>
  <files>src/components/ProjectCard.tsx</files>
  <read_first>src/components/ProjectCard.tsx</read_first>
  <action>
  В `src/components/ProjectCard.tsx`:

  1. Добавить импорт: `import { TelegramChatPreview } from './TelegramChatPreview';`
  2. Заменить telegram placeholder на реальный компонент:

  ```tsx
  {project.type === 'telegram' && (
    <TelegramChatPreview
      title={project.title}
      botUsername={project.botUsername}
      chatScript={project.chatScript}
    />
  )}
  ```

  Также удалить `.previewPlaceholder` стиль если он больше не используется (проверить `.css` модуль).

  Упростить `.preview` в ProjectCard.module.css — убрать min-height (теперь высоту задают сами preview-компоненты):

  Изменение в `ProjectCard.module.css`:
  ```css
  .preview {
    /* Контейнер-слот для preview компонентов — они сами держат размер */
  }
  ```

  Можно вообще удалить пустой селектор `.preview` и убрать соответствующий `className` из TSX — **но** оставим пустой класс на будущее (для общих стилей типа маргинов).
  </action>
  <verify>`npm run dev` — все 4 карточки на месте: 2 web с SVG placeholder, 2 telegram с живыми чат-мокапами</verify>
  <acceptance_criteria>
    - ProjectCard рендерит TelegramChatPreview для type='telegram'
    - Оба TG-бота показывают сценарии диалогов из chatScript
    - Все 4 кнопки клавиатуры видны в первом сообщении marketplace-kwork (['Разработка','Дизайн'],['Тексты','Переводы'],['Все категории'])
    - build/lint/typecheck зелёные
  </acceptance_criteria>
  <done>TG превью интегрированы</done>
</task>

</tasks>

<verification>
- [ ] build/lint/typecheck зелёные
- [ ] `npm run dev`: Projects секция показывает:
  - tezis + frontend-seven с SVG placeholder (видео не добавлены)
  - marketplace-kwork с сценарием /start → категории → разработка → сортировка
  - ticket-seller с сценарием /start → афиша → концерт → места
- [ ] TG-мокапы: bot bubble слева с dim border, user bubble справа в phosphor background
- [ ] Keyboard кнопки отрисованы внутри bot-сообщений
- [ ] Footer "preview — not a live bot" виден в каждом TG-мокапе
- [ ] Klик [open in telegram] открывает https://t.me/test_marketplace_kwork_bot / ticket_seller
- [ ] Screen reader читает TG-мокап как "Dialog preview for ... bot"
</verification>

<success_criteria>

- Все 3 плана Phase 4 применены
- Секция Projects полноценная: метаданные + живые превью (веб = SVG до добавления видео, TG = React мокапы)
- Архитектура позволяет владельцу добавить новый проект только правкой src/data/projects.ts

</success_criteria>

<output>
`.planning/phases/04-project-showcase/04-03-SUMMARY.md`
</output>
