---
phase: 03-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/TerminalPrompt.tsx
  - src/components/TerminalPrompt.module.css
  - src/App.tsx
  - src/styles/globals.css
autonomous: true
requirements:
  - CONT-05
user_setup: []
must_haves:
  truths:
    - "Навбар отображает terminal-prompt-формат: guest@portfolio:~$ cd <section>"
    - "Активная команда подсвечивается инверсией (aria-current='location')"
    - "Рядом с активной командой мигает <Cursor>"
    - "На узких экранах (< 480px) prompt сокращается до ~$ cd <section>"
    - "Нав доступен с клавиатуры (Tab/Shift+Tab, Enter активирует ссылку)"
  artifacts:
    - "src/components/TerminalPrompt.tsx — компонент с 3 ссылками"
    - "src/components/TerminalPrompt.module.css — стили prompt-like формата"
    - "src/App.tsx — рендерит TerminalPrompt вместо inline <nav>"
  key_links:
    - "TerminalPrompt получает SECTION_IDS и active id как props ИЛИ использует useActiveSection сам"
    - "globals.css теряет временные <nav> стили (они теперь в CSS Module)"
---

<objective>
Заменить минимальный навбар Phase 1 на полноценный `<TerminalPrompt>` в стиле shell-промпта. Визуальное наполнение фразы «это терминал» — ключевой CRT-differentiator.

Output: Новый компонент в продакшн-готовом виде, заменяющий nav в App.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/research/FEATURES.md
@.planning/phases/03-content-sections/CONTEXT.md
@src/App.tsx
@src/styles/globals.css
@src/types/sections.ts
@src/hooks/useActiveSection.ts
@src/components/Cursor.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: TerminalPrompt.module.css</name>
  <files>src/components/TerminalPrompt.module.css</files>
  <read_first>.planning/phases/03-content-sections/CONTEXT.md §TerminalPrompt (D-16..20)</read_first>
  <action>
  Создать `src/components/TerminalPrompt.module.css`:

  ```css
  .prompt {
    position: sticky;
    top: 0;
    z-index: 10;
    background-color: var(--bg);
    padding: var(--space-2) var(--space-4);
    border-bottom: 1px solid var(--phosphor-dim);
    display: flex;
    align-items: center;
    gap: var(--space-2);
    flex-wrap: wrap;
    font-family: var(--font-mono);
  }

  .userHost {
    color: var(--phosphor-dim);
  }

  .userHost::after {
    content: ':~$';
    color: var(--phosphor);
    margin-left: 0;
  }

  .commands {
    display: flex;
    gap: var(--space-2);
    flex-wrap: wrap;
    align-items: center;
    list-style: none;
  }

  .separator {
    color: var(--phosphor-dim);
    user-select: none;
  }

  .command {
    color: var(--phosphor);
    text-decoration: none;
    padding: 0 var(--space-1);
  }

  .command:hover,
  .command:focus-visible {
    background-color: var(--phosphor);
    color: var(--bg);
    outline: none;
  }

  .command[aria-current='location'] {
    background-color: var(--phosphor);
    color: var(--bg);
  }

  .activeCursor {
    margin-left: var(--space-1);
  }

  /* Узкий экран: прячем user@host, оставляем ~$ */
  @media (max-width: 480px) {
    .userHost {
      display: none;
    }
    .userHost::before {
      content: '~';
      color: var(--phosphor-dim);
      display: inline;
    }
    .prompt::before {
      content: '~$';
      color: var(--phosphor);
      margin-right: var(--space-1);
    }
  }
  ```

  Ограничения:
  - НЕ использовать иконки (Lucide etc.) — только текстовые знаки
  - Separator `|` не в CSS pseudo-element, а как реальный span — чтобы было семантически видно (и чтобы screen reader корректно пропускал при `aria-hidden`)
  </action>
  <verify>CSS модуль валиден</verify>
  <acceptance_criteria>
    - .prompt — sticky top, z-index 10
    - .command[aria-current='location'] имеет инверсию (bg phosphor, color bg)
    - @media (max-width: 480px) прячет .userHost и показывает префикс ~$
    - Никаких background-image/иконок
  </acceptance_criteria>
  <done>Стили готовы</done>
</task>

<task type="auto">
  <name>Task 2: TerminalPrompt.tsx компонент</name>
  <files>src/components/TerminalPrompt.tsx</files>
  <read_first>src/hooks/useActiveSection.ts, src/types/sections.ts, src/components/Cursor.tsx</read_first>
  <action>
  Создать `src/components/TerminalPrompt.tsx`:

  ```tsx
  import { Fragment } from 'react';
  import styles from './TerminalPrompt.module.css';
  import { SECTION_IDS, SECTION_LABELS } from '../types/sections';
  import { useActiveSection } from '../hooks/useActiveSection';
  import { Cursor } from './Cursor';

  const USER_HOST = 'guest@portfolio';

  export function TerminalPrompt() {
    const active = useActiveSection();

    return (
      <nav aria-label="Primary" className={styles.prompt}>
        <span className={styles.userHost} aria-hidden="true">
          {USER_HOST}
        </span>
        <ul className={styles.commands}>
          {SECTION_IDS.map((id, i) => (
            <Fragment key={id}>
              {i > 0 && (
                <li aria-hidden="true" className={styles.separator}>
                  |
                </li>
              )}
              <li>
                <a
                  href={`#${id}`}
                  className={styles.command}
                  aria-current={active === id ? 'location' : undefined}
                >
                  cd {SECTION_LABELS[id]}
                </a>
                {active === id && (
                  <span className={styles.activeCursor}>
                    <Cursor />
                  </span>
                )}
              </li>
            </Fragment>
          ))}
        </ul>
      </nav>
    );
  }
  ```

  ВАЖНО:
  - `USER_HOST` имеет `aria-hidden` — SR не нужен префикс, важна только команда
  - Separator `|` тоже `aria-hidden` — визуальный разделитель, SR пропускает
  - `useActiveSection` остаётся в этом компоненте — App.tsx не должен прокидывать props (избегаем лишнего re-render App)
  - Named export
  </action>
  <verify>typecheck + lint</verify>
  <acceptance_criteria>
    - Использует useActiveSection
    - Рендерит USER_HOST с aria-hidden
    - Separators имеют aria-hidden="true"
    - Активная ссылка имеет aria-current="location" И рядом рендерится <Cursor />
    - Все 3 секции из SECTION_IDS выведены
  </acceptance_criteria>
  <done>Компонент готов</done>
</task>

<task type="auto">
  <name>Task 3: Интеграция в App.tsx и чистка globals.css</name>
  <files>src/App.tsx, src/styles/globals.css</files>
  <read_first>src/App.tsx, src/styles/globals.css</read_first>
  <action>
  **App.tsx** — заменить `<nav>` на `<TerminalPrompt />`. Удалить неиспользуемый импорт `SECTION_IDS`, `SECTION_LABELS` (они теперь внутри TerminalPrompt). `useActiveSection` уже не нужен в App — перенесли в TerminalPrompt. `useHashNavigation` остаётся.

  Итоговый App.tsx:
  ```tsx
  import { AboutSection } from './sections/AboutSection';
  import { ProjectsSection } from './sections/ProjectsSection';
  import { ContactSection } from './sections/ContactSection';
  import { CRTShell } from './components/CRTShell';
  import { TerminalPrompt } from './components/TerminalPrompt';
  import { useHashNavigation } from './hooks/useHashNavigation';

  export function App() {
    useHashNavigation();

    return (
      <CRTShell>
        <TerminalPrompt />
        <main>
          <AboutSection />
          <ProjectsSection />
          <ContactSection />
        </main>
      </CRTShell>
    );
  }
  ```

  **globals.css** — удалить следующие блоки (они теперь в TerminalPrompt.module.css):
  ```
  nav[aria-label='Primary'] { ... }
  nav[aria-label='Primary'] ul { ... }
  nav[aria-label='Primary'] a[aria-current='location'] { ... }
  ```

  Остальные глобальные правила (body, h1, h2, a, section, focus-visible, main) — оставить.
  </action>
  <verify>`npm run dev` — навбар в новом формате, активная команда подсвечена и с курсором; клики работают; на узком экране (DevTools responsive) user@host прячется</verify>
  <acceptance_criteria>
    - App.tsx больше не импортирует SECTION_IDS/SECTION_LABELS/useActiveSection
    - App.tsx импортирует TerminalPrompt
    - globals.css не содержит селектор `nav[aria-label='Primary']`
    - `npm run build/lint/typecheck` зелёные
    - Визуал: prompt-like, активная команда + курсор рядом
  </acceptance_criteria>
  <done>Интеграция завершена</done>
</task>

</tasks>

<verification>
- [ ] Все build/lint/typecheck зелёные
- [ ] Навбар показывает `guest@portfolio:~$ cd about | cd projects | cd contact`
- [ ] Активная команда инвертирована + мигающий курсор рядом
- [ ] DevTools responsive < 480px: `guest@portfolio` спрятан, остался `~$`
- [ ] Tab переводит фокус на каждую команду в порядке; focus-ring виден
- [ ] Screen reader: читает только команды (user@host и разделители скрыты)
</verification>

<success_criteria>

- TerminalPrompt — единственный навбар
- Accessibility не регрессировала по сравнению с Phase 1

</success_criteria>

<output>
`.planning/phases/03-content-sections/03-01-SUMMARY.md`
</output>
