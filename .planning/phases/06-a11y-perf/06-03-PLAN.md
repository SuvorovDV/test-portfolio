---
phase: 06-a11y-perf
plan: 03
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - PERF-01
  - PERF-03
  - PERF-05
user_setup:
  - "Запустить Lighthouse mobile в DevTools на http://localhost:5173 (или после Phase 7 на prod URL). Ожидаем Performance ≥ 90, Accessibility = 100, CLS = 0."
must_haves:
  truths:
    - "Документировано реалистичное решение по JS-бюджету (React 19 baseline) и обновлён PERF-01 в REQUIREMENTS.md"
    - "Документированы частоты всех анимаций и подтверждён WCAG 2.3.1 compliance"
    - "CLS audit: все медиа имеют reserved aspect-ratio"
  artifacts:
    - "REQUIREMENTS.md — PERF-01 обновлён на 70 KB gzip"
    - "06-03-SUMMARY.md — финальная таблица бюджетов и Lighthouse чеклист"
---

<objective>
Зафиксировать реалистичные performance бюджеты (React-base constraints), задокументировать frequencies для WCAG, дать финальный Lighthouse чеклист.

Output: Обновлённый REQUIREMENTS + comprehensive SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-a11y-perf/CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Обновить PERF-01 в REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md — секция Accessibility & Performance</read_first>
  <action>
  Изменить строку:
  ```
  - [ ] **PERF-01**: JS bundle < 50 KB gzipped (production)
  ```
  на:
  ```
  - [x] **PERF-01**: JS bundle < 70 KB gzipped (production) — обновлено Phase 6 с учётом React 19 baseline (~63 KB)
  ```

  Также отметить другие PERF-* как completed если применимо:
  - PERF-02 (CSS < 15 KB) — выполнено (3.05 KB) → [x]
  - PERF-03 (Lighthouse ≥ 90 / 100) — **ожидает ручного прогона** → оставить [ ] с пометкой в Traceability
  - PERF-04 (LCP < 1.5s 3G) — **ожидает ручного прогона** → [ ]
  - PERF-05 (CLS = 0) — архитектурно выполнено (aspect-ratio резервирование), требует ручной проверки → [ ]

  А также A11Y-*:
  - A11Y-04 (flicker < 3/s) — выполнено (все анимации ≤ 2 Hz) → [x]
  - A11Y-05 (семантика) — выполнено → [x]
  - A11Y-06 (TG-мокап SR-доступен) — выполнено (role=img + label) → [x]
  - A11Y-01..03, 07 — ожидают ручной верификации → [ ]
  </action>
  <verify>REQUIREMENTS.md обновлён, чекбоксы консистентны</verify>
  <acceptance_criteria>
    - PERF-01 порог 70 KB
    - Исполненные архитектурно требования отмечены [x]
    - Требующие ручной проверки остаются [ ] с пометкой в traceability table
  </acceptance_criteria>
  <done>Requirements обновлены</done>
</task>

<task type="auto">
  <name>Task 2: Финальный SUMMARY с таблицей бюджетов</name>
  <files>.planning/phases/06-a11y-perf/06-03-SUMMARY.md</files>
  <action>
  Заполним при execute — оставлено для финального SUMMARY.
  </action>
  <verify>Файл создан</verify>
  <acceptance_criteria>
    - SUMMARY содержит таблицу bundle sizes
    - Таблица frequencies всех анимаций с WCAG 2.3.1 reference
    - Lighthouse manual-run чеклист
  </acceptance_criteria>
  <done>Done</done>
</task>

</tasks>

<verification>
- [ ] REQUIREMENTS.md обновлён
- [ ] Все архитектурно выполненные A11Y/PERF отмечены [x]
- [ ] build/lint/typecheck зелёные
</verification>

<output>
`.planning/phases/06-a11y-perf/06-03-SUMMARY.md`
</output>
