---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - tsconfig.node.json
  - vite.config.ts
  - eslint.config.js
  - .prettierrc.json
  - .prettierignore
  - .gitignore
  - .gitattributes
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/vite-env.d.ts
  - src/components/.gitkeep
  - src/sections/.gitkeep
  - src/hooks/.gitkeep
  - src/data/.gitkeep
  - src/styles/.gitkeep
  - src/types/.gitkeep
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
user_setup: []
must_haves:
  truths:
    - "`npm run dev` запускает Vite dev-сервер без ошибок"
    - "`npm run build` создаёт `dist/index.html` и `dist/assets/*`"
    - "`npm run lint` завершается с кодом 0"
    - "`npm run typecheck` (`tsc --noEmit`) завершается с кодом 0"
    - "TypeScript strict включён: `strict: true` И `noUncheckedIndexedAccess: true` в tsconfig"
  artifacts:
    - package.json — содержит скрипты dev/build/preview/lint/typecheck/format
    - tsconfig.json — strict mode
    - vite.config.ts — с комментом о будущем `base` для GH Pages
    - eslint.config.js — flat config (НЕ .eslintrc)
    - .prettierrc.json
    - index.html — с `<div id="root">`
    - src/main.tsx — точка входа, StrictMode
    - src/App.tsx — минимальный плейсхолдер (перепишется в 01-02)
  key_links:
    - "package.json scripts → выполняются без ошибок"
    - "tsconfig.json include пути → соответствуют структуре src/"
---

<objective>
Bootstrap Vite + React 19 + TypeScript strict проект с ESLint flat config + Prettier. Создать директорную структуру под будущие компоненты/секции/хуки.

Purpose: Дать Phase 2 (Theming) и всем последующим чистую почву: строгие типы, линтер, форматер, работающие build/dev скрипты.
Output: Инициализированный проект, `npm run dev` открывает минимальную страницу, все чеки зелёные.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/phases/01-foundation/CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Инициализировать package.json и установить зависимости</name>
  <files>package.json, package-lock.json</files>
  <read_first>.planning/research/STACK.md</read_first>
  <action>
  Создать package.json со следующим содержимым (поля `name`, `private: true`, `type: "module"`, `version: "0.0.0"`).

  Установить зависимости через npm (пинить мажоры по STACK.md, минорные — latest):
  - dependencies: `react@^19`, `react-dom@^19`
  - devDependencies:
    - `@types/react@^19`, `@types/react-dom@^19`
    - `@vitejs/plugin-react@^5`
    - `vite@^6`
    - `typescript@^5.7`
    - `@eslint/js@^9`
    - `eslint@^9`
    - `typescript-eslint@^8`
    - `eslint-plugin-react-hooks@^5`
    - `eslint-plugin-react-refresh@^0.4`
    - `globals@^15`
    - `prettier@^3`

  Добавить scripts:
  - `"dev": "vite"`
  - `"build": "tsc -b && vite build"`
  - `"preview": "vite preview"`
  - `"lint": "eslint ."`
  - `"typecheck": "tsc --noEmit"`
  - `"format": "prettier --write \"src/**/*.{ts,tsx,css}\""`

  Конкретный путь: использовать `npm install` (не pnpm/yarn). Если версия пакета на npm отличается от указанной в STACK.md (например, вышел major 10), принять latest major, но сохранить совместимость (читать STACK.md §versions).
  </action>
  <verify>npm install завершается без ошибок; `cat package.json` содержит все указанные scripts</verify>
  <acceptance_criteria>
    - package.json содержит `"type": "module"`
    - package.json содержит все 6 скриптов: dev, build, preview, lint, typecheck, format
    - dependencies содержит react и react-dom
    - devDependencies содержит vite, typescript, eslint, prettier
    - package-lock.json создан
  </acceptance_criteria>
  <done>npm run dev ещё не запускаем — в task 2 сделаем конфиги</done>
</task>

<task type="auto">
  <name>Task 2: Настроить TypeScript (strict)</name>
  <files>tsconfig.json, tsconfig.node.json, src/vite-env.d.ts</files>
  <read_first>.planning/phases/01-foundation/CONTEXT.md §Implementation Decisions D-02</read_first>
  <action>
  Создать `tsconfig.json` с composite references на `tsconfig.app.json` и `tsconfig.node.json` (стандартный Vite 6 паттерн).

  Либо упростить до одного `tsconfig.json` (пользователь — соло-разработчик, references избыточны). Рекомендую упрощённую версию:

  ```json
  {
    "compilerOptions": {
      "target": "ES2022",
      "useDefineForClassFields": true,
      "lib": ["ES2022", "DOM", "DOM.Iterable"],
      "module": "ESNext",
      "skipLibCheck": true,
      "moduleResolution": "bundler",
      "allowImportingTsExtensions": true,
      "resolveJsonModule": true,
      "isolatedModules": true,
      "moduleDetection": "force",
      "noEmit": true,
      "jsx": "react-jsx",
      "strict": true,
      "noUnusedLocals": true,
      "noUnusedParameters": true,
      "noFallthroughCasesInSwitch": true,
      "noUncheckedIndexedAccess": true,
      "noImplicitOverride": true,
      "exactOptionalPropertyTypes": true
    },
    "include": ["src"]
  }
  ```

  Создать `tsconfig.node.json` для vite.config.ts:
  ```json
  {
    "compilerOptions": {
      "target": "ES2022",
      "lib": ["ES2023"],
      "module": "ESNext",
      "moduleResolution": "bundler",
      "allowSyntheticDefaultImports": true,
      "strict": true,
      "noEmit": true
    },
    "include": ["vite.config.ts"]
  }
  ```

  Создать `src/vite-env.d.ts`:
  ```ts
  /// <reference types="vite/client" />
  ```
  </action>
  <verify>`npx tsc --noEmit` завершается без ошибок (пока нет src файлов — только vite-env.d.ts)</verify>
  <acceptance_criteria>
    - tsconfig.json содержит `"strict": true`
    - tsconfig.json содержит `"noUncheckedIndexedAccess": true`
    - tsconfig.json содержит `"exactOptionalPropertyTypes": true`
    - src/vite-env.d.ts существует
  </acceptance_criteria>
  <done>tsc --noEmit зелёный</done>
</task>

<task type="auto">
  <name>Task 3: Настроить Vite</name>
  <files>vite.config.ts, index.html</files>
  <read_first>.planning/research/STACK.md §Build/Deploy, .planning/research/PITFALLS.md §GH Pages deploy</read_first>
  <action>
  Создать `vite.config.ts`:
  ```ts
  import { defineConfig } from 'vite';
  import react from '@vitejs/plugin-react';

  // base: для GitHub Pages нужно '/test-portfolio/' (имя репо).
  // Настраивается в Phase 7 (Deploy). Пока оставляем '/' для dev.
  export default defineConfig({
    plugins: [react()],
    base: '/',
  });
  ```

  Создать `index.html` в корне:
  ```html
  <!doctype html>
  <html lang="ru">
    <head>
      <meta charset="UTF-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <title>test-portfolio</title>
    </head>
    <body>
      <div id="root"></div>
      <script type="module" src="/src/main.tsx"></script>
    </body>
  </html>
  ```

  НЕ вставлять favicon / og-мета-теги / CRT-стили — это Phase 7 и Phase 2.
  </action>
  <verify>vite.config.ts парсится без ошибок (`npx vite build --mode development` не упадёт на parse)</verify>
  <acceptance_criteria>
    - vite.config.ts импортирует `@vitejs/plugin-react` и использует `plugins: [react()]`
    - vite.config.ts содержит `base: '/'` с комментарием про Phase 7 GH Pages
    - index.html содержит `<div id="root">` и `<script type="module" src="/src/main.tsx">`
    - index.html `<html lang="ru">`
  </acceptance_criteria>
  <done>Структура Vite готова</done>
</task>

<task type="auto">
  <name>Task 4: ESLint flat config + Prettier</name>
  <files>eslint.config.js, .prettierrc.json, .prettierignore</files>
  <read_first>.planning/research/STACK.md §Dev tooling</read_first>
  <action>
  Создать `eslint.config.js` (flat config, ESM):
  ```js
  import js from '@eslint/js';
  import globals from 'globals';
  import reactHooks from 'eslint-plugin-react-hooks';
  import reactRefresh from 'eslint-plugin-react-refresh';
  import tseslint from 'typescript-eslint';

  export default tseslint.config(
    { ignores: ['dist', '.planning', '.claude'] },
    {
      extends: [js.configs.recommended, ...tseslint.configs.recommended],
      files: ['**/*.{ts,tsx}'],
      languageOptions: {
        ecmaVersion: 2022,
        globals: globals.browser,
      },
      plugins: {
        'react-hooks': reactHooks,
        'react-refresh': reactRefresh,
      },
      rules: {
        ...reactHooks.configs.recommended.rules,
        'react-refresh/only-export-components': ['warn', { allowConstantExport: true }],
      },
    },
  );
  ```

  Создать `.prettierrc.json`:
  ```json
  {
    "singleQuote": true,
    "semi": true,
    "printWidth": 100,
    "trailingComma": "all",
    "arrowParens": "always"
  }
  ```

  Создать `.prettierignore`:
  ```
  dist
  .planning
  .claude
  package-lock.json
  ```
  </action>
  <verify>`npm run lint` завершается с кодом 0 (пока нет src/*.ts файлов кроме vite-env.d.ts)</verify>
  <acceptance_criteria>
    - eslint.config.js существует и экспортирует default
    - eslint.config.js импортирует `typescript-eslint` и `eslint-plugin-react-hooks`
    - .prettierrc.json содержит `"singleQuote": true`
    - НЕТ файла `.eslintrc.json` / `.eslintrc.js` (только flat config)
  </acceptance_criteria>
  <done>npm run lint зелёный</done>
</task>

<task type="auto">
  <name>Task 5: Создать src/main.tsx и минимальный App</name>
  <files>src/main.tsx, src/App.tsx</files>
  <read_first>.planning/phases/01-foundation/CONTEXT.md §Specific Ideas</read_first>
  <action>
  Создать `src/main.tsx`:
  ```tsx
  import { StrictMode } from 'react';
  import { createRoot } from 'react-dom/client';
  import { App } from './App';

  const rootEl = document.getElementById('root');
  if (!rootEl) throw new Error('Root element #root not found');

  createRoot(rootEl).render(
    <StrictMode>
      <App />
    </StrictMode>,
  );
  ```

  Создать `src/App.tsx` — минимальный placeholder. Структура секций будет нормально сделана в Plan 01-02. Здесь достаточно:
  ```tsx
  export function App() {
    return (
      <main>
        <h1>test-portfolio</h1>
        <p>Phase 1 placeholder — shell comes in 01-02.</p>
      </main>
    );
  }
  ```

  ЗАМЕТКА: никаких inline стилей, никакой CRT-стилизации. Phase 2 перепишет.
  </action>
  <verify>`npm run build` завершается успешно; `npm run dev` запускает сервер и страница открывается</verify>
  <acceptance_criteria>
    - src/main.tsx использует StrictMode и createRoot
    - src/main.tsx имеет null-check на root element
    - src/App.tsx экспортирует named export `App` (не default — консистентность с ESLint react-refresh правилом)
    - npm run build проходит
    - npm run lint проходит
    - npm run typecheck проходит
  </acceptance_criteria>
  <done>Работающий минимальный Vite+React app</done>
</task>

<task type="auto">
  <name>Task 6: Создать директорную структуру и .gitignore/.gitattributes</name>
  <files>src/components/.gitkeep, src/sections/.gitkeep, src/hooks/.gitkeep, src/data/.gitkeep, src/styles/.gitkeep, src/types/.gitkeep, .gitignore, .gitattributes</files>
  <action>
  Создать пустые директории с `.gitkeep`:
  - src/components/.gitkeep
  - src/sections/.gitkeep
  - src/hooks/.gitkeep
  - src/data/.gitkeep
  - src/styles/.gitkeep
  - src/types/.gitkeep

  Создать `.gitignore` (если не создался при `npm init`):
  ```
  node_modules
  dist
  dist-ssr
  *.local
  .DS_Store
  *.log
  .vscode/*
  !.vscode/settings.json
  !.vscode/extensions.json
  ```

  Создать `.gitattributes` (помогает с CRLF/LF на Windows):
  ```
  * text=auto eol=lf
  *.png binary
  *.jpg binary
  *.woff2 binary
  ```
  </action>
  <verify>`ls src/` показывает все 6 папок; `git status` не ругается на CRLF предупреждения</verify>
  <acceptance_criteria>
    - src/{components,sections,hooks,data,styles,types}/.gitkeep существуют
    - .gitignore содержит node_modules и dist
    - .gitattributes существует и содержит `* text=auto eol=lf`
  </acceptance_criteria>
  <done>Структура готова к Plan 01-02</done>
</task>

</tasks>

<verification>
Перед объявлением плана завершённым:
- [ ] `npm run dev` открывает страницу без ошибок в консоли браузера
- [ ] `npm run build` создаёт `dist/index.html` и `dist/assets/*.js/css`
- [ ] `npm run lint` код выхода 0
- [ ] `npm run typecheck` код выхода 0
- [ ] `git status` чистый (все новые файлы застейджены и/или в .gitignore)
- [ ] Структура `src/{components,sections,hooks,data,styles,types}` существует
</verification>

<success_criteria>

- Все 6 задач завершены
- Все verification чеки пройдены
- Никаких CRT-стилей, никакого контента секций — это Phase 2/3
- Предупреждения ESLint отсутствуют (ошибки тоже)

</success_criteria>

<output>
После завершения создать `.planning/phases/01-foundation/01-01-SUMMARY.md` с:
- Финальными версиями установленных пакетов (из package.json)
- Подтверждением зелёных scripts (dev/build/lint/typecheck)
- Любые отклонения от плана и почему
</output>
