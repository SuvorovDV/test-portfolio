---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/App.tsx
  - src/sections/AboutSection.tsx
  - src/sections/ProjectsSection.tsx
  - src/sections/ContactSection.tsx
  - src/hooks/useActiveSection.ts
  - src/hooks/useHashNavigation.ts
  - src/hooks/useReducedMotion.ts
  - src/types/sections.ts
autonomous: true
requirements:
  - FOUND-05
user_setup: []
must_haves:
  truths:
    - "Страница рендерит три секции с id=about, id=projects, id=contact в указанном порядке"
    - "Клик по ссылке с href='#about' скроллит к секции About и обновляет URL hash"
    - "Back/forward браузера корректно переводит между секциями (popstate handler)"
    - "useActiveSection возвращает id текущей видимой секции"
    - "useReducedMotion возвращает boolean, синхронный с `matchMedia('(prefers-reduced-motion: reduce)')`"
    - "smooth scrolling используется при reduced-motion=false, мгновенный — при true"
  artifacts:
    - src/App.tsx — рендерит три `<section id>` с navbar-линками
    - src/sections/AboutSection.tsx — placeholder, примет контент в Phase 3
    - src/sections/ProjectsSection.tsx — placeholder
    - src/sections/ContactSection.tsx — placeholder
    - src/hooks/useActiveSection.ts — IntersectionObserver на 3 секции
    - src/hooks/useHashNavigation.ts — click/popstate handlers
    - src/hooks/useReducedMotion.ts — matchMedia wrapper
    - src/types/sections.ts — константы `SECTION_IDS` и тип `SectionId`
  key_links:
    - "App.tsx импортирует все 3 секции и оба хука"
    - "Клик-линки указывают на `#about`/`#projects`/`#contact`"
    - "useActiveSection наблюдает за ref'ами секций, обновляющийся state доступен navbar'у"
---

<objective>
Поднять работающий скелет одностраничника с тремя секциями и hash-навигацией. Задать базовую семантику (`<main>`, `<section aria-labelledby>`, `<nav>`) и реактивную подсветку активной секции — всё это переиспользуется в Phase 2+.

Purpose: С этого момента любая фаза, которая пишет контент/стили/компоненты, может полагаться на существующую структуру секций и готовые хуки.
Output: App.tsx + три Section-компонента + три хука. Никакой стилизации (Phase 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-foundation/CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Константы и типы секций</name>
  <files>src/types/sections.ts</files>
  <action>
  Создать `src/types/sections.ts`:
  ```ts
  export const SECTION_IDS = ['about', 'projects', 'contact'] as const;
  export type SectionId = (typeof SECTION_IDS)[number];

  export const SECTION_LABELS: Record<SectionId, string> = {
    about: 'about',
    projects: 'projects',
    contact: 'contact',
  };
  ```

  Идея: единый источник правды. App.tsx и хуки импортируют отсюда, никаких magic strings.
  </action>
  <verify>typecheck проходит</verify>
  <acceptance_criteria>
    - SECTION_IDS — `readonly` tuple из ['about', 'projects', 'contact']
    - SectionId — union type 'about' | 'projects' | 'contact'
  </acceptance_criteria>
  <done>Типы готовы</done>
</task>

<task type="auto">
  <name>Task 2: useReducedMotion хук</name>
  <files>src/hooks/useReducedMotion.ts</files>
  <read_first>.planning/research/ARCHITECTURE.md §Accessibility architecture, .planning/research/PITFALLS.md §Accessibility pitfalls</read_first>
  <action>
  Создать `src/hooks/useReducedMotion.ts`:
  ```ts
  import { useEffect, useState } from 'react';

  const QUERY = '(prefers-reduced-motion: reduce)';

  export function useReducedMotion(): boolean {
    const [reduced, setReduced] = useState(() => {
      if (typeof window === 'undefined') return false;
      return window.matchMedia(QUERY).matches;
    });

    useEffect(() => {
      const mql = window.matchMedia(QUERY);
      const handler = (e: MediaQueryListEvent) => setReduced(e.matches);
      mql.addEventListener('change', handler);
      return () => mql.removeEventListener('change', handler);
    }, []);

    return reduced;
  }
  ```

  ВАЖНО: lazy init state (прочитать matchMedia один раз при mount), cleanup на unmount.
  </action>
  <verify>typecheck + lint зелёные</verify>
  <acceptance_criteria>
    - useReducedMotion возвращает boolean
    - useEffect cleanup удаляет listener
    - Lazy init через callback в useState
  </acceptance_criteria>
  <done>Хук готов, используется в task 4</done>
</task>

<task type="auto">
  <name>Task 3: useActiveSection хук (IntersectionObserver)</name>
  <files>src/hooks/useActiveSection.ts</files>
  <read_first>.planning/research/ARCHITECTURE.md §Component inventory — useActiveSection</read_first>
  <action>
  Создать `src/hooks/useActiveSection.ts`:
  ```ts
  import { useEffect, useState } from 'react';
  import { SECTION_IDS, type SectionId } from '../types/sections';

  export function useActiveSection(): SectionId {
    const [active, setActive] = useState<SectionId>(SECTION_IDS[0]);

    useEffect(() => {
      const elements = SECTION_IDS
        .map((id) => document.getElementById(id))
        .filter((el): el is HTMLElement => el !== null);

      if (elements.length === 0) return;

      const observer = new IntersectionObserver(
        (entries) => {
          // Берём entry с наибольшим intersection ratio
          const visible = entries
            .filter((e) => e.isIntersecting)
            .sort((a, b) => b.intersectionRatio - a.intersectionRatio);
          if (visible.length > 0 && visible[0]) {
            const id = visible[0].target.id;
            if ((SECTION_IDS as readonly string[]).includes(id)) {
              setActive(id as SectionId);
            }
          }
        },
        {
          // -40% сверху/снизу → секция считается активной когда она в центральной 20% viewport
          rootMargin: '-40% 0px -40% 0px',
          threshold: [0, 0.25, 0.5, 0.75, 1],
        },
      );

      elements.forEach((el) => observer.observe(el));
      return () => observer.disconnect();
    }, []);

    return active;
  }
  ```
  </action>
  <verify>typecheck + lint зелёные</verify>
  <acceptance_criteria>
    - Возвращает SectionId (не string)
    - observer.disconnect() в cleanup
    - rootMargin использует отрицательные проценты (середина viewport)
  </acceptance_criteria>
  <done>Хук готов</done>
</task>

<task type="auto">
  <name>Task 4: useHashNavigation хук (smooth scroll + popstate)</name>
  <files>src/hooks/useHashNavigation.ts</files>
  <read_first>.planning/phases/01-foundation/CONTEXT.md §Navigation D-09 D-10</read_first>
  <action>
  Создать `src/hooks/useHashNavigation.ts`:
  ```ts
  import { useEffect } from 'react';
  import { SECTION_IDS, type SectionId } from '../types/sections';
  import { useReducedMotion } from './useReducedMotion';

  function isSectionId(value: string): value is SectionId {
    return (SECTION_IDS as readonly string[]).includes(value);
  }

  export function useHashNavigation(): void {
    const reducedMotion = useReducedMotion();

    useEffect(() => {
      const behavior: ScrollBehavior = reducedMotion ? 'auto' : 'smooth';

      const scrollToHash = (hash: string) => {
        const id = hash.replace(/^#/, '');
        if (!isSectionId(id)) return;
        const el = document.getElementById(id);
        if (el) el.scrollIntoView({ behavior, block: 'start' });
      };

      const onClick = (e: MouseEvent) => {
        const target = e.target as HTMLElement | null;
        const anchor = target?.closest('a');
        if (!anchor) return;
        const href = anchor.getAttribute('href');
        if (!href || !href.startsWith('#')) return;
        const id = href.slice(1);
        if (!isSectionId(id)) return;
        e.preventDefault();
        history.pushState(null, '', href);
        scrollToHash(href);
      };

      const onPopState = () => {
        if (window.location.hash) scrollToHash(window.location.hash);
      };

      // Initial scroll if hash present on load
      if (window.location.hash) {
        // Небольшая задержка чтобы успел отрендериться DOM
        queueMicrotask(() => scrollToHash(window.location.hash));
      }

      document.addEventListener('click', onClick);
      window.addEventListener('popstate', onPopState);

      return () => {
        document.removeEventListener('click', onClick);
        window.removeEventListener('popstate', onPopState);
      };
    }, [reducedMotion]);
  }
  ```

  ВАЖНО:
  - delegated click-handler на document (один listener, не per-link)
  - `history.pushState` + manual scroll, чтобы НЕ полагаться на дефолтный браузерный behavior (который игнорирует `scroll-behavior: smooth` в CSS для некоторых случаев)
  - Cleanup обоих listeners
  </action>
  <verify>typecheck + lint зелёные</verify>
  <acceptance_criteria>
    - useHashNavigation — void hook (без return value)
    - Cleanup удаляет ОБА listeners
    - preventDefault() вызывается только для валидных section hashes
    - Использует useReducedMotion для выбора behavior
  </acceptance_criteria>
  <done>Хук готов</done>
</task>

<task type="auto">
  <name>Task 5: Section-компоненты (placeholders)</name>
  <files>src/sections/AboutSection.tsx, src/sections/ProjectsSection.tsx, src/sections/ContactSection.tsx</files>
  <action>
  Создать три plaeholder секции. Все одинаковые по структуре, отличаются только id и title.

  `src/sections/AboutSection.tsx`:
  ```tsx
  export function AboutSection() {
    return (
      <section id="about" aria-labelledby="about-heading">
        <h2 id="about-heading">about</h2>
        <p>Placeholder — content comes in Phase 3.</p>
      </section>
    );
  }
  ```

  `src/sections/ProjectsSection.tsx`:
  ```tsx
  export function ProjectsSection() {
    return (
      <section id="projects" aria-labelledby="projects-heading">
        <h2 id="projects-heading">projects</h2>
        <p>Placeholder — gallery comes in Phase 4.</p>
      </section>
    );
  }
  ```

  `src/sections/ContactSection.tsx`:
  ```tsx
  export function ContactSection() {
    return (
      <section id="contact" aria-labelledby="contact-heading">
        <h2 id="contact-heading">contact</h2>
        <p>Placeholder — content comes in Phase 3.</p>
      </section>
    );
  }
  ```

  Named export'ы (не default) — консистентно с App.
  </action>
  <verify>typecheck + lint зелёные</verify>
  <acceptance_criteria>
    - Три файла, три named export'а
    - Каждая секция имеет `id=<section-id>` и `aria-labelledby=<section-id>-heading`
    - Каждый `<h2>` имеет соответствующий id
  </acceptance_criteria>
  <done>Секции готовы</done>
</task>

<task type="auto">
  <name>Task 6: Переписать App.tsx — navbar + секции</name>
  <files>src/App.tsx</files>
  <read_first>src/types/sections.ts, src/hooks/useActiveSection.ts, src/hooks/useHashNavigation.ts</read_first>
  <action>
  Переписать `src/App.tsx`:
  ```tsx
  import { AboutSection } from './sections/AboutSection';
  import { ProjectsSection } from './sections/ProjectsSection';
  import { ContactSection } from './sections/ContactSection';
  import { useActiveSection } from './hooks/useActiveSection';
  import { useHashNavigation } from './hooks/useHashNavigation';
  import { SECTION_IDS, SECTION_LABELS } from './types/sections';

  export function App() {
    const active = useActiveSection();
    useHashNavigation();

    return (
      <>
        <nav aria-label="Primary">
          <ul>
            {SECTION_IDS.map((id) => (
              <li key={id}>
                <a href={`#${id}`} aria-current={active === id ? 'location' : undefined}>
                  cd {SECTION_LABELS[id]}
                </a>
              </li>
            ))}
          </ul>
        </nav>
        <main>
          <AboutSection />
          <ProjectsSection />
          <ContactSection />
        </main>
      </>
    );
  }
  ```

  ВАЖНО:
  - `aria-current="location"` для активной ссылки — пригодится для стилизации и screen reader'ам (Phase 2)
  - `cd <label>` — осмысленный префикс в духе терминала, но без стилизации (будет применена в Phase 3 через `<TerminalPrompt>`)
  - Никаких `className` / `style` — чистая семантика
  </action>
  <verify>`npm run dev` запускается, клик по навбару скроллит к секции, URL меняет hash, back/forward работают</verify>
  <acceptance_criteria>
    - App.tsx использует useActiveSection и useHashNavigation
    - Рендерит `<nav>` и `<main>`
    - Linkи в навбаре `href="#about"`, `href="#projects"`, `href="#contact"`
    - Активный link имеет `aria-current="location"`
    - npm run build, lint, typecheck все зелёные
  </acceptance_criteria>
  <done>Работающий скелет с навигацией</done>
</task>

</tasks>

<verification>
Перед объявлением плана завершённым:
- [ ] `npm run dev` — страница открывается, три секции видны в порядке About / Projects / Contact
- [ ] Клик по `cd projects` в навбаре скроллит к projects, URL становится `.../#projects`
- [ ] Back в браузере возвращает hash и скролл к about
- [ ] При скролле вручную активный пункт навбара меняется (проверить в DevTools: `<a aria-current="location">`)
- [ ] В DevTools > Rendering > Emulate CSS media feature → prefers-reduced-motion: reduce → скролл становится мгновенным
- [ ] `npm run build`, `npm run lint`, `npm run typecheck` — все 0
- [ ] Никаких ошибок/warnings в консоли браузера
</verification>

<success_criteria>

- Все 6 задач завершены
- Все verification чеки пройдены
- Никакой стилизации — это Phase 2
- 3 хука переиспользуемы (Phase 2-6 будут использовать useReducedMotion и useActiveSection)

</success_criteria>

<output>
После завершения создать `.planning/phases/01-foundation/01-02-SUMMARY.md`:
- Подтверждение что Foundation готова
- Список компонентов/хуков с их назначением (для референса следующих фаз)
- Любые отклонения от плана
</output>
