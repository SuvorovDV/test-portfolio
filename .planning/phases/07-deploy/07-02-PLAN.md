---
phase: 07-deploy
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - .github/workflows/deploy.yml
autonomous: true
requirements:
  - DEPLOY-02
  - DEPLOY-07
user_setup:
  - "Создать публичный репо github.com/SuvorovDV/test-portfolio"
  - "git remote add origin git@github.com:SuvorovDV/test-portfolio.git && git push -u origin master:main"
  - "В Settings → Pages → Build and deployment → Source: **GitHub Actions** (не branch)"
  - "После первого push — дождаться зелёного run Actions → открыть https://suvorovdv.github.io/test-portfolio/"
must_haves:
  truths:
    - ".github/workflows/deploy.yml использует официальные GH Pages actions (configure-pages, upload-pages-artifact, deploy-pages)"
    - "Workflow триггерится на push в main и workflow_dispatch"
    - "Permissions: contents: read, pages: write, id-token: write"
    - "Concurrency group = 'pages', cancel-in-progress: false"
    - "Build job: checkout → setup-node 20 → npm ci → npm run build → upload-pages-artifact (path: dist)"
    - "Deploy job: needs build, environment: github-pages, deploy-pages@v4"
  artifacts:
    - ".github/workflows/deploy.yml"
---

<objective>
Автоматический деплой `dist/` на GitHub Pages при каждом push в main.

Output: Рабочий CI pipeline + пройденный публичный URL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-deploy/CONTEXT.md
@.planning/phases/07-deploy/07-01-SUMMARY.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: .github/workflows/deploy.yml</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
  Создать `.github/workflows/deploy.yml`:

  ```yaml
  name: Deploy to GitHub Pages

  on:
    push:
      branches: [main]
    workflow_dispatch:

  permissions:
    contents: read
    pages: write
    id-token: write

  concurrency:
    group: pages
    cancel-in-progress: false

  jobs:
    build:
      runs-on: ubuntu-latest
      steps:
        - name: Checkout
          uses: actions/checkout@v4

        - name: Setup Node.js
          uses: actions/setup-node@v4
          with:
            node-version: '20'
            cache: 'npm'

        - name: Install dependencies
          run: npm ci

        - name: Typecheck
          run: npm run typecheck

        - name: Lint
          run: npm run lint

        - name: Build
          run: npm run build

        - name: Setup Pages
          uses: actions/configure-pages@v5

        - name: Upload artifact
          uses: actions/upload-pages-artifact@v3
          with:
            path: ./dist

    deploy:
      needs: build
      runs-on: ubuntu-latest
      environment:
        name: github-pages
        url: ${{ steps.deployment.outputs.page_url }}
      steps:
        - name: Deploy to GitHub Pages
          id: deployment
          uses: actions/deploy-pages@v4
  ```

  ВАЖНО:
  - Версии actions на апрель 2026: checkout@v4, setup-node@v4, configure-pages@v5, upload-pages-artifact@v3, deploy-pages@v4 (текущие стабильные на момент планирования)
  - `npm ci` — строгий install из package-lock.json
  - typecheck + lint до build — fail fast если регрессия
  - `environment: github-pages` обязателен для deploy-pages
  - Два jobs (build + deploy) — best practice для GH Pages
  - Concurrency group 'pages' — предотвращает parallel deploys
  </action>
  <verify>Файл создан; YAML валидный (проверить `yamllint` или visual inspection)</verify>
  <acceptance_criteria>
    - Файл существует в .github/workflows/deploy.yml
    - Triggers: push to main + workflow_dispatch
    - Permissions минимальный набор (3 штуки)
    - Concurrency group pages, cancel-in-progress false
    - Build job: checkout, setup-node@v4 с node 20, npm ci, typecheck, lint, build, configure-pages, upload-pages-artifact
    - Deploy job: needs build, environment github-pages, deploy-pages@v4
  </acceptance_criteria>
  <done>Workflow готов</done>
</task>

<task type="auto">
  <name>Task 2: README-level hints для владельца</name>
  <files>.planning/phases/07-deploy/DEPLOY-STEPS.md</files>
  <action>
  Создать вспомогательный документ с пошаговым гайдом. Claude не может сам создать GitHub репо / нажать кнопки в Settings, поэтому выводим инструкцию.

  `.planning/phases/07-deploy/DEPLOY-STEPS.md`:

  ```markdown
  # Deploy steps (manual)

  После завершения 07-01 и 07-02 в репо — выполнить вручную:

  ## 1. Создать публичный репо на GitHub

  https://github.com/new
  - Owner: SuvorovDV
  - Repository name: **test-portfolio**
  - Visibility: **Public**
  - НЕ инициализировать README/gitignore/license (у нас уже всё есть)

  ## 2. Переименовать локальную ветку в main (опц.)

  ```bash
  git branch -M main
  ```

  ## 3. Связать и залить

  ```bash
  git remote add origin git@github.com:SuvorovDV/test-portfolio.git
  git push -u origin main
  ```

  (HTTPS вариант: `https://github.com/SuvorovDV/test-portfolio.git`)

  ## 4. Включить GitHub Pages через Actions

  https://github.com/SuvorovDV/test-portfolio/settings/pages
  - **Build and deployment → Source**: выбрать **GitHub Actions**
  - Сохранить

  ## 5. Дождаться workflow

  https://github.com/SuvorovDV/test-portfolio/actions
  - Первый run стартует автоматически после push
  - Ждать "Deploy to GitHub Pages" → зелёный
  - URL появится в job summary и в Settings → Pages

  ## 6. Открыть сайт

  **https://suvorovdv.github.io/test-portfolio/**

  Проверить:
  - Загружается без 404
  - VT323 шрифт применяется
  - Все 4 карточки проекта видны
  - Навбар работает, клик по cd projects — скроллит
  - DevTools → View Source: meta-теги присутствуют
  - В Telegram отправь URL — preview карточка должна появиться (может взять до ~1 часа кэш)

  ## Известные issues

  - **OG в Telegram** — SVG may not render. Fallback: сконвертировать `public/og.svg` → `public/og.png` (1200×630) через resvg/rsvg-convert/онлайн-конвертер. Обновить og:image в index.html.
  - **Если 404 на ассеты** — проверить что base в vite.config === имени репо (включая точный case: `test-portfolio`, не `Test-Portfolio`)
  ```
  </action>
  <verify>Файл создан</verify>
  <acceptance_criteria>
    - Пошаговая инструкция с конкретными URL и командами
    - Упомянуто переключение Source на GitHub Actions в Settings → Pages
    - Секция known issues
  </acceptance_criteria>
  <done>Guide готов</done>
</task>

</tasks>

<verification>
- [ ] .github/workflows/deploy.yml создан и синтаксически валиден
- [ ] DEPLOY-STEPS.md создан
- [ ] `npm run build` ещё раз — dist/ готов
- [ ] Локальный `npm run preview` — сайт работает на базе localhost:4173

После push (ручной шаг владельца):
- [ ] GitHub Actions прогоняет build+deploy без ошибок
- [ ] https://suvorovdv.github.io/test-portfolio/ открывается
- [ ] Все секции работают
- [ ] Meta-теги корректны (Facebook Sharing Debugger / Twitter Card Validator)
</verification>

<output>
`.planning/phases/07-deploy/07-02-SUMMARY.md`
</output>
